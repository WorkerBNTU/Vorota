target-version = "py311"
line-length = 100
exclude = ["venv", "migrations", "staticfiles", "media"]

[lint]
# Критичные ошибки + типичные баги; стиль (E501 и т.п.) не блокируем CI
select = ["E9", "F63", "F7", "F82", "F401", "F811", "F821", "F841"]
